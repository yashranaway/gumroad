export interface SidebarCategory {
  title: string;
  slug: string;
  url: string;
}

export interface ArticleLink {
  title: string;
  url: string;
}

export interface ArticleCategory {
  title: string;
  slug: string;
  url: string;
}
